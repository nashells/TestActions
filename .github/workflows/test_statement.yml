name: Test Statement

on:
    workflow_dispatch:

jobs:
    test:
        runs-on: ubuntu-latest

        steps:
            - name: test statement
              run: |
                if [[ "docs/ArchitectureDocument/RMS.md" =~ "docs/" ]]; then
                    echo "Matched"
                elif [[ "docs/ArchitectureDocument/RMS.md" =~ "docs/ArchitectureDocument/" ]]; then
                    echo "Sub Matched"
                else
                    echo "Not Matched"
                fi